package org.assetsglobal.repository;

import org.assetsglobal.entity.Brocker;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BrockerRepository extends JpaRepository<Brocker, Integer>{

}
